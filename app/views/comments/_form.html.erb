<h3> Add a comment </h3>
<%= bootstrap_form_for([@post, @post.comments.build]) do |f| %>
<%= f.text_field :name %><br>
<br>
<%= f.text_area :body %><br>
<br>
<%= f.submit :class => 'btn btn-success' %>
<% end %>
